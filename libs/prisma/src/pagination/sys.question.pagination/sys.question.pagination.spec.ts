import { SysQuestionPagination } from './sys.question.pagination';

describe('SysQuestionPagination', () => {
  it('should be defined', () => {
    expect(new SysQuestionPagination()).toBeDefined();
  });
});
