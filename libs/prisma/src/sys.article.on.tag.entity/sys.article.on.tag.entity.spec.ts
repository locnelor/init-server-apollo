import { SysArticleOnTagEntity } from './sys.article.on.tag.entity';

describe('SysArticleOnTagEntity', () => {
  it('should be defined', () => {
    expect(new SysArticleOnTagEntity()).toBeDefined();
  });
});
