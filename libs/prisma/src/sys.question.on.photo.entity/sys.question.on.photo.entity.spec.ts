import { SysQuestionOnPhotoEntity } from './sys.question.on.photo.entity';

describe('SysQuestionOnPhotoEntity', () => {
  it('should be defined', () => {
    expect(new SysQuestionOnPhotoEntity()).toBeDefined();
  });
});
